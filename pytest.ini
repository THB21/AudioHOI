[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs =
    .git
    .venv
    build
    dist
    third_party
    vendor
addopts = --strict-config --strict-markers
markers =
    repository_data: requires generated repository data that is intentionally not tracked by Git
